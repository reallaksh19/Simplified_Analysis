import assert from 'node:assert/strict';
import { calculateLocalAttachmentScreening } from '../src/core/local-attachment-screening/index.js';
import { screeningRequestFixture } from './lafea.2-fixtures.mjs';
let request=screeningRequestFixture(),result=calculateLocalAttachmentScreening(request);
for(const envelope of result.envelopes){const field=fieldOf(envelope.quantity),values=result.pointStressStates.map((row)=>row.stressTensor[field]),expected=envelope.quantity.endsWith('Minimum')||envelope.quantity==='principalMinimum'?Math.min(...values):Math.max(...values);assert.equal(envelope.value,expected);const source=result.pointStressStates.find((row)=>row.screeningCaseId===envelope.screeningCaseId&&row.evaluationLocationId===envelope.evaluationLocationId);assert.equal(envelope.value,source.stressTensor[field]);assert.deepEqual(envelope.sourceLoadCaseTerms,source.sourceLoadCaseTerms);}
const tied=screeningRequestFixture((raw)=>{raw.screeningCases=[{screeningCaseId:'B',mechanicalTerms:[],pressureDefinitionId:'P-OPEN',pressureFactor:0,sourceReference:'B'},{screeningCaseId:'A',mechanicalTerms:[],pressureDefinitionId:'P-OPEN',pressureFactor:0,sourceReference:'A'}];raw.evaluationLocations=[{...raw.evaluationLocations[0],evaluationLocationId:'Z'},{...raw.evaluationLocations[0],evaluationLocationId:'A'}];});result=calculateLocalAttachmentScreening(tied);assert.ok(result.envelopes.every((row)=>row.screeningCaseId==='A'&&row.evaluationLocationId==='A'));
assert.throws(()=>screeningRequestFixture((raw)=>raw.screeningCases.push({...raw.screeningCases[0]})),/Duplicate screeningCaseId/);assert.throws(()=>screeningRequestFixture((raw)=>raw.evaluationLocations.push({...raw.evaluationLocations[0]})),/Duplicate evaluationLocationId/);assert.throws(()=>screeningRequestFixture((raw)=>raw.screeningCases[0].mechanicalTerms.push({...raw.screeningCases[0].mechanicalTerms[0]})),/Duplicate loadCaseId/);
console.log('LAFEA.2 deterministic envelopes, independent extrema, provenance, ties and duplicate rejection passed.');
function fieldOf(quantity){if(quantity==='vonMisesMaximum')return 'vonMises';if(quantity.startsWith('principal'))return quantity;return quantity.replace(/Maximum$|Minimum$/,'');}
