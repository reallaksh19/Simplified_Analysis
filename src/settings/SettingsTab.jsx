import React from 'react';

export const SettingsTab = () => {
  return (
    <div style={{ padding: '24px', color: '#fff' }}>
      <h2>Settings / Defaults</h2>
      <p>Configure default units, material defaults, warning thresholds, and report project metadata here.</p>
    </div>
  );
};
